pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Build stage running...'  // ✅ Just prints a message
            }
        }
        stage('Test') {
            steps {
                echo 'Test stage running...'   // ✅ Just prints a message
            }
        }
    }
}
